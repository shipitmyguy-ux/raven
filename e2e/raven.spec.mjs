import { test, expect } from "@playwright/test";
test("Raven shell loads and core tracks are usable",async({page})=>{const errors=[];page.on("pageerror",e=>errors.push(e.message));await page.goto("/");await expect(page.locator("#jobs")).toBeVisible();await expect(page.locator("#jobList")).toBeVisible();for(const track of ["Games / 3D","Professional","Labor","Wildcard"]){const tab=page.locator('[data-track="'+track+'"]');await expect(tab).toBeVisible();await tab.click();}expect(errors).toEqual([]);});
test("document review UI is present",async({page})=>{await page.goto("/");await expect(page.locator("#documentReviewDialog")).toBeAttached();await expect(page.locator("#reviewInstructions")).toBeAttached();await expect(page.locator("#reviewSubmit")).toBeAttached();});
test("options and master resume controls are present",async({page})=>{await page.goto("/");await page.locator("#optionsButton").click();await expect(page.locator("#optionsDialog")).toBeVisible();await expect(page.locator("#addMasterResumeButton")).toBeAttached();await expect(page.locator("#masterResumeList")).toBeAttached();});
